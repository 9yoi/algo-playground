// helper: check for overlap
  // if leftX_A + width <= leftX_B, return A

// helper: find left object
  // take smaller leftX, return index of obj
// helper: find right object
  // take larger leftX, return index of obj
// helper: width
  // leftX_left + width - leftX_right
// helper: height
  // bottomY_right - (bottomY_left + height)

function getOverlap () {
// Get coordinates of bottom left

// Get width and height  
}


